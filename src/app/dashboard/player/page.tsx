import { PlayerDashboard } from '@/features/panel/player/dashboard';

export const metadata = {
  title: 'My Dashboard - FutsalBook',
  description: 'Manage your bookings, teams, and favorites',
};

export default function PlayerDashboardPage() {
  return <PlayerDashboard />;
}
